# =============================================================================
# GitHub Star Helper - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control
# =============================================================================

# -----------------------------------------------------------------------------
# Part 1: 必需配置 (REQUIRED)
# -----------------------------------------------------------------------------
# 以下配置是系统运行所必需的，必须填写才能使用核心功能

# GitHub Token (REQUIRED for sync functionality)
# ⚠️ READ-ONLY TOKEN - No modification permissions needed
#
# Create token at: https://github.com/settings/tokens
# Token name: "GitHub Star Helper" (or any name you prefer)
#
# Required scopes (CHECK ONLY THESE):
# ☑️ public_repo  - Read-only access to public repositories
#
# Optional scopes (only if you have private starred repos):
# ☑️ repo         - Read-only access to your private repositories
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# Part 2: 进阶功能配置 (ADVANCED FEATURES)
# -----------------------------------------------------------------------------
# 以下配置用于启用进阶功能，按需取消注释并填写

# [AI 聊天和仓库分析] - 需要配置 OpenAI API
# 启用后可使用：
#   - 💬 AI 对话助手
#   - 🤖 仓库智能分析（初始化时自动分类）
#   - 🔍 重新分析单个仓库
# LLM_MODEL=gpt-4o-mini
# LLM_API_KEY=your_openai_api_key_here
# LLM_BASE_URL=https://api.openai.com/v1  # Optional: 自定义 API 端点

# [语义搜索] - 需要安装和配置 Ollama
# 启用后可使用：
#   - 🎯 概念搜索（理解"机器学习"能搜到"深度学习"）
#   - 🌐 跨语言搜索（中文查询匹配英文内容）
#   - 🔤 同义词识别（"并发"和"并行"等同义词）
# 前置条件：
#   1. 安装 Ollama: curl -fsSL https://ollama.com/install.sh | sh
#   2. 拉取模型: ollama pull bge-m3
#   3. 启动服务: ollama serve
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_EMBEDDING_MODEL=bge-m3
# OLLAMA_TIMEOUT=30
# CHROMADB_PATH=data/chromadb

# GitHub Username (可选，用于同步功能)
# GITHUB_USER=your_github_username

# -----------------------------------------------------------------------------
# Part 3: 基础配置 (BASIC CONFIGURATION)
# -----------------------------------------------------------------------------
# 以下配置有合理的默认值，通常不需要修改

# -----------------------------------------------------------------------------
# API Server
# -----------------------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8889

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
DB_TYPE=sqlite
SQLITE_PATH=data/github_stars.db
