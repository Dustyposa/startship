# =============================================================================
# GitHub Star Helper - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
DB_TYPE=sqlite
SQLITE_PATH=data/starship.db

# PostgreSQL (optional, only if using PostgreSQL instead of SQLite)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=starship
# POSTGRES_USER=starship
# POSTGRES_PASSWORD=

# -----------------------------------------------------------------------------
# LLM Configuration
# -----------------------------------------------------------------------------
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini
LLM_API_KEY=your_openai_api_key_here
# LLM_BASE_URL=https://api.openai.com/v1  # Optional: override default API URL
LLM_TIMEOUT=60

# -----------------------------------------------------------------------------
# GitHub Configuration (REQUIRED for sync functionality)
# -----------------------------------------------------------------------------
# ⚠️ READ-ONLY TOKEN - No modification permissions needed
#
# Create token at: https://github.com/settings/tokens
# Token name: "GitHub Star Helper" (or any name you prefer)
#
# Required scopes (CHECK ONLY THESE):
# ☑️ public_repo  - Read-only access to public repositories
#
# Optional scopes (only if you have private starred repos):
# ☑️ repo         - Read-only access to your private repositories
#
# ⚠️ DO NOT check these (NOT NEEDED):
# ☐ repo:status     - Commit status
# ☐ repo_deployment - Deployment status
# ☐ public_repo     - Write access (NOT needed!)
# ☐ delete_repo     - Delete repos (NOT needed!)
# ☐ any write scopes
#
# This token will ONLY:
# - Read your starred repositories list
# - Read repository metadata (stars, forks, description, etc.)
# - It will NEVER modify, create, or delete anything
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# Storage
# -----------------------------------------------------------------------------
README_STORAGE_PATH=data/readmes

# -----------------------------------------------------------------------------
# Concurrency Settings
# -----------------------------------------------------------------------------
MAX_CONCURRENT_LLM=5
MAX_CONCURRENT_GITHUB=10

# -----------------------------------------------------------------------------
# Pagination
# -----------------------------------------------------------------------------
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
